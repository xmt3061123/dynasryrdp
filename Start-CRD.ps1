param (
    [string]$authcode,
    [string]$pin
)

# 在这里添加启动 Chrome Remote Desktop 的代码
Write-Output "Starting Chrome Remote Desktop with authcode: $authcode and pin: $pin"

# 示例命令，替换为实际命令
# Start-ChromeRemoteDesktop -AuthCode $authcode -Pin $pin
